package com.clip.matching.repository.projection;

import com.clip.matching.entity.OnethingKeyword;

public record FirstParticipantKeywordDto(Long matchingId,
                                         OnethingKeyword keyWord) {
}
