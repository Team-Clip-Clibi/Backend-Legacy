spring:
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      enabled: false
  profiles:
    include: office-data

eureka:
  client:
    enabled: false

api:
  url: http://localhost:8080

server:
  port: 8081
