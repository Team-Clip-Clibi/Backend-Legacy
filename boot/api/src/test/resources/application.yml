spring:
  jpa:
    hibernate:
      ddl-auto: create
  batch:
    job:
        enabled: false
discord:
  webhook-url: Https://localhost:123/mock

exclude-auth-path-patterns:
  paths:
    - path-pattern: /test
      method: POST